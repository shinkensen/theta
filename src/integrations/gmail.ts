import{invoke}from"@tauri-apps/api/core";import type{IntegrationStatus}from"./types";
export const gmail={status:()=>invoke<IntegrationStatus>("gmail_status"),saveClientId:(clientId:string)=>invoke<void>("gmail_set_client_id",{clientId}),connect:(capabilities:string[]=[])=>invoke<string>("gmail_connect",{capabilities}),disconnect:()=>invoke<void>("gmail_disconnect"),profile:()=>invoke<unknown>("gmail_get_profile"),search:(query:string,maxResults=20)=>invoke<unknown>("gmail_search_messages",{query,maxResults}),message:(messageId:string)=>invoke<unknown>("gmail_get_message",{messageId}),thread:(threadId:string)=>invoke<unknown>("gmail_get_thread",{threadId}),draft:(to:string,subject:string,body:string)=>invoke<unknown>("gmail_create_draft",{to,subject,body}),send:(to:string,subject:string,body:string)=>invoke<unknown>("gmail_send_message",{to,subject,body}),modify:(messageId:string,addLabelIds:string[],removeLabelIds:string[])=>invoke<unknown>("gmail_modify_message",{messageId,addLabelIds,removeLabelIds})};
