import{useCallback,useEffect,useState,type FormEvent}from"react";import{gmail}from"../../integrations/gmail";import type{IntegrationStatus}from"../../integrations/types";import{CredentialField,IntegrationCard}from"./IntegrationCard";
export function GmailSettingsCard(){const[status,setStatus]=useState<IntegrationStatus|null>(null),[clientId,setClientId]=useState(""),[compose,setCompose]=useState(false),[send,setSend]=useState(false),[modify,setModify]=useState(false),[message,setMessage]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(true);const refresh=useCallback(async()=>{setLoading(true);setError("");try{setStatus(await gmail.status())}catch(e){setError(String(e))}finally{setLoading(false)}},[]);useEffect(()=>{void refresh()},[refresh]);const save=async(e:FormEvent)=>{e.preventDefault();setError("");try{await gmail.saveClientId(clientId);setClientId("");setMessage("Client ID saved locally.");await refresh()}catch(r){setError(String(r))}};const connect=async()=>{setError("");setMessage("Waiting for Google sign-in in your browser…");try{const caps=[compose&&"compose",send&&"send",modify&&"modify"].filter((v):v is string=>Boolean(v));const label=await gmail.connect(caps);setMessage(`Connected as ${label}.`);await refresh()}catch(r){setMessage("");setError(String(r))}};const disconnect=async()=>{if(!window.confirm("Disconnect Gmail from Theta?"))return;try{await gmail.disconnect();setMessage("Gmail disconnected.");await refresh()}catch(r){setError(String(r))}};return <IntegrationCard name="Gmail" category="MAIL" description="Search and read mail by default. Additional sensitive scopes are requested only when selected." configured={status?.configured} connected={status?.connected} loading={loading} error={error} message={message} accountLabel={status?.accountLabel} onSubmit={save}><CredentialField label="Google Desktop OAuth Client ID" value={clientId} onChange={setClientId} placeholder={status?.configured?"Enter a new ID to replace it":"Paste Client ID"} required={!status?.configured}/><label className="toggle"><input type="checkbox" checked={compose} onChange={e=>setCompose(e.target.checked)}/><span/>Allow draft creation</label><label className="toggle"><input type="checkbox" checked={send} onChange={e=>setSend(e.target.checked)}/><span/>Allow sending mail</label><label className="toggle"><input type="checkbox" checked={modify} onChange={e=>setModify(e.target.checked)}/><span/>Allow archive and label changes</label><small>Google may require app verification for Gmail scopes. Sending and mailbox changes always require Theta approval.</small><div className="button-row"><button disabled={loading||!clientId.trim()}>Save Client ID</button><button type="button" className="primary" disabled={loading||!status?.configured} onClick={()=>void connect()}>Connect</button>{status?.connected&&<button type="button" className="danger" onClick={()=>void disconnect()}>Disconnect</button>}</div></IntegrationCard>}
