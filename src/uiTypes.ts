export type { Settings, CalEvent, CalendarSummary, ProcessInfo, SystemStats, PortInfo, RagDoc, ProfileItem, ProfileCategory } from "./agent/tools";
export interface AuthStatus { connected: boolean; has_credentials: boolean; email?: string }
